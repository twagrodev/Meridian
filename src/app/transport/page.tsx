import { ModulePlaceholder } from "@/components/shared/ModulePlaceholder";

export default function TransportPage() {
  return (
    <ModulePlaceholder
      title="Transport"
      description="Multi-modal transport tracking, route management, and dispatch coordination will appear here."
      theme="transport"
    />
  );
}
