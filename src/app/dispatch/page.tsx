import { ModulePlaceholder } from "@/components/shared/ModulePlaceholder";

export default function DispatchPage() {
  return (
    <ModulePlaceholder
      title="Dispatch"
      description="Dispatch planning, scheduling, and calendar view will appear here."
    />
  );
}
